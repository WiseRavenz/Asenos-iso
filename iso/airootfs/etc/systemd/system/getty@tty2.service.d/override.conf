[Unit]
Description=Autologin root on tty2 (drop-in)

[Service]
# Clear existing ExecStart from the template then set autologin for root on tty2
ExecStart=
ExecStart=-/sbin/agetty --autologin root --noclear %I $TERM
